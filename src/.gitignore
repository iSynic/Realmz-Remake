.dropbox.attr
Thumbs.db
.import/*
.godot/*
dev/*
demo/*
# Source-adjacent Godot 4 import metadata is versioned. The compiled cache
# remains ignored above.

# Controlled scenario fixtures intentionally preserve their exact package
# contents, so do not add generated script UID sidecars inside them.
scripts/scenario_runtime/tests/fixtures/**/*.gd.uid
default_bus_layout.tres
default_env.tres
godot-mod-player.png
icon.png
project.godot
TestScene.tscn
TestScene.gd
global.gd
build/*

# MinGW runtime DLLs (downloaded during build)
addons/godot-openmpt/bin/runtime/*.dll
